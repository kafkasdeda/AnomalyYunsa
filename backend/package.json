{
  "name": "backend",
  "version": "1.0.0",
  "description": "FastAPI backend for fabric quality control",
  "scripts": {
    "dev": "python -m uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "start": "python -m uvicorn main:app --host 0.0.0.0 --port 8000",
    "test": "python -m pytest tests/ -v",
    "lint": "python -m black . && python -m isort .",
    "clean": "find . -type d -name __pycache__ -delete"
  },
  "private": true
}